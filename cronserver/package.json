{
  "name": "cronserver",
  "version": "0.0.1",
  "type": "module",
  "description": "Runs the change checks on a schedule using cron",
  "main": "checkChanges.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hamish Starling",
  "dependencies": {
    "nodemailer": "^6.7.8"
  },
  "devDependencies": {
    "dotenv": "^16.0.2"
  }
}
